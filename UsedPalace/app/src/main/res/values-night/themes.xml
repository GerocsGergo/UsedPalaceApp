<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Base.Theme.UsedPalace" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="rippleColor">@color/colorPrimaryRipple</item>

    </style>

    <style name="ThemeOverlay.UsedPalace.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/colorBackground</item>
        <item name="fullscreenTextColor">@color/colorText</item>
    </style>
</resources>